mutation Payment($operatorId: String!, $phoneNumber: String!, $amount: Int!) {
  pay(operatorId: $operatorId, phoneNumber: $phoneNumber, amount: $amount) {
    success
    operatorId
    balance
  }
}
